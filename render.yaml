services:
  - type: web
    name: recivia-voice
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
